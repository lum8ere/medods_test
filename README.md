# Task Service

Сервис для управления задачами с HTTP API на Go.

## Основные возможности
- **Гибкая периодичность:** создание задач на каждый $N$-й день, конкретные числа месяца, четные/нечетные дни или список выбранных дат.
- **Масштабируемость:** физическое создание каждой задачи в базе данных.
- **Производительность:** пагинация списков и пакетная вставка данных.
- **Документация:** интерактивный Swagger UI для тестирования.

## Как это спроектировано (Обоснование и допущения)

При разработке я сделал несколько архитектурных предположений, чтобы решение максимально закрывало потребности врачей:

### 1. Почему каждая задача в серии — это отдельная запись?
Когда врач создает периодическое правило, система физически создает в базе данных все копии задач на 30 дней вперед.
*   **Обоснование:** В медицине важна гибкость. Врач должен иметь возможность зайти в задачу на конкретный вторник, поменять там статус или добавить комментарий (например, "зайти к пациенту позже"), не ломая при этом всю остальную серию задач. Одной "виртуальной" записью такого добиться невозможно.

### 2. Логика коротких месяцев
*   **Допущение:** Если задача назначена на 31-е число месяца, а в текущем месяце всего 30 или 28 дней (февраль), система автоматически переносит выполнение на последний доступный день этого месяца. Так задачи не "пропадают" из расписания.

### 3. Ограничения безопасности (Safety Limits)
*   **Допущение:** Чтобы один запрос случайно не создал миллион записей в базе (например, если кто-то ошибется в настройках), я ввел жесткий лимит — **не более 100 задач** за один раз. Это защищает систему от перегрузки.

### 4. Пагинация
*   **Обоснование:** В реальной клинике задач могут быть тысячи. Метод получения списка возвращает объект с метаданными (`total_count`, `limit`, `offset`), чтобы фронтенд мог легко построить постраничную навигацию.

## Требования

- Go `1.23+`
- Docker и Docker Compose

## Быстрый запуск через Docker Compose

Я разделил инфраструктуру на три части: база данных, автоматический мигратор и само приложение.

```bash
docker compose up --build
```

**Важно:** Теперь за миграции отвечает отдельный сервис `migrate`. Приложение `app` запустится только после того, как все таблицы будут успешно созданы и обновлены.

После запуска сервис будет доступен по адресу `http://localhost:8080`.

Если нужно полностью сбросить базу данных и начать с чистого листа:
```bash
docker compose down -v
docker compose up --build
```

## Swagger

Swagger UI:

```text
http://localhost:8080/swagger/
```

OpenAPI JSON:

```text
http://localhost:8080/swagger/openapi.json
```

### Основные маршруты (`/api/v1`):
- `POST /tasks` — создать задачу или серию (принимает объект `recurrence`).
- `GET /tasks` — получить список (фильтры `start`, `end`, пагинация `limit`, `offset`).
- `GET /tasks/{id}` — подробности задачи.
- `PUT /tasks/{id}` — обновление текста, статуса или даты.
- `DELETE /tasks/{id}` — удаление конкретного экземпляра задачи.

## Что можно улучшить (TODO, если успею до просмотра, сделаю :))
- **Background Worker:** В текущем виде задачи создаются на месяц вперед. В продакшене нужен фоновый процесс (cron), который будет раз в сутки "докидывать" новые задачи в базу для активных правил.
- **Soft Delete:** Для медицинской системы полезно не удалять задачи физически, а помечать их как "удаленные" (`deleted_at`), чтобы сохранить историю назначений для аудита.
- **Таймзоны:** Добавить поддержку часовых поясов, чтобы врачи в разных регионах видели задачи по своему локальному времени.
- **Контроль наложений (Conflicts):** Система должна предупреждать врача, если на одно и то же время у него запланировано несколько критически важных дел.
- **Отчеты:** Выгрузка статистики: сколько задач было выполнено вовремя, сколько просрочено и какова средняя нагрузка на конкретного врача в неделю.